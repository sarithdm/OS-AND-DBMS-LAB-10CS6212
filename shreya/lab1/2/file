#checking the existing of file in a directory

cd "$1"
for var in "$1";
do 
    if [ -a "$2" ];
    then
    echo "file '$2' exist in $1 "
    else
    echo "file '$2' does not exist in $1"
    fi
done 
   
